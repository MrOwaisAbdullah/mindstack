"""Public surface for `apps.mcp_proxy`.

Currently empty — the proxy view is mounted directly from `config.urls` and
no other app needs to call into mcp_proxy programmatically. Phase 4 may add
helpers here once OAuth lands.
"""
